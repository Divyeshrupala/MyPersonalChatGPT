# Multi-AI Assistant Environment Configuration

# Server Configuration
PORT=3000
NODE_ENV=development

# Production Configuration (for Render deployment)
# APP_URL=https://chat.divyeshrupala.in

# Note: API keys are now provided by users through the interface!
# No server-side API keys needed for normal operation.

# Optional: Server-side fallback API keys (not recommended)
# Only use these if you want to provide fallback keys when users don't have their own
# OPENAI_API_KEY=your_openai_key_here
# GEMINI_API_KEY=your_gemini_key_here  
# GROQ_API_KEY=your_groq_key_here
# DEEPSEEK_API_KEY=your_deepseek_key_here
# STABILITY_API_KEY=your_stability_key_here
# OPENROUTER_API_KEY=your_openrouter_key_here

# Production Settings for Render Deployment
# TRUST_PROXY=true

# Optional: Additional server configuration
# MAX_FILE_SIZE=5242880  # 5MB in bytes
# UPLOAD_DIR=uploads

# Rate Limiting Settings
# RATE_LIMIT_WINDOW=900000  # 15 minutes
# RATE_LIMIT_MAX=200        # Max requests per window
# CHAT_RATE_LIMIT_WINDOW=60000  # 1 minute
# CHAT_RATE_LIMIT_MAX=30    # Max chat requests per window