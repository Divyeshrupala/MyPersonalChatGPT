# Production Environment Configuration
# Copy this to .env on your production server (Render)

NODE_ENV=production
PORT=10000
APP_URL=https://chat.divyeshrupala.in

# Note: API keys are provided by users through the interface
# No server-side API keys needed!

# Optional: Rate limiting (defaults are fine for most cases)
# RATE_LIMIT_WINDOW=900000
# RATE_LIMIT_MAX=200
# CHAT_RATE_LIMIT_WINDOW=60000
# CHAT_RATE_LIMIT_MAX=30
